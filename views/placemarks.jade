kml(xmlns='http://www.opengis.net/kml/2.2')
  Document
    for val,key in marks
      Placemark
        name=val.name
        Point
          coordinates= key
        description
          for ev in val.events
            | #{ev._datestr} 
            - if (ev.url)
              a(href=ev.url.value)= ev.summary.value
            - else
              span= ev.summary.value
            br